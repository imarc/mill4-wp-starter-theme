{# This file demonstrates using most of the index.twig template and modifying 
   just a small part. See `search.twig` for an example of another approach #}
{% extends "index.twig" %}

{% block content %}
	{% include 'partials/generic-hero.twig' with {
        title: 'Resources',
    } %}
	<div class="container">
		<div class="archive grid -columns-3">
			<aside class="archive__sidebar cell -span-1">
				<form method="get" action="">
					<h4>Filter by Type</h4>
					{% for term in resource_types %}
						{% set checked = term.slug in current_filters %}
						<label class="field -inline">
							<input type="checkbox" name="resource_type[]" value="{{ term.slug }}" {{ checked ? 'checked' }}>
							{{ term.name }}
						</label>
					{% endfor %}
					<button type="submit" class="button">Apply</button>
				</form>
			</aside>

			<div class="archive__content cell -span-2">
				{{ parent() }}
			</div>
		</div>
	</div>
{% endblock %}

<div class="featureCards">
    <div class="featureCards__cards">
        {% for card in block.cards %}
            {% include 'partials/card.twig' with { card: card } %}
        {% endfor %}
    </div>
</div>